//
// Created by Dmitry Velychko on 12.07.2024.
//

#include "../../include/utils/XMPFormat.h"

std::string XMPFormat::encode(const unsigned char *data, std::size_t dataSize) {

    return std::string();
}

std::vector<unsigned char> XMPFormat::decode(std::string data) {
    //double* a = &vec[0];

    //double arr[100];
    //std::copy(v.begin(), v.end(), arr);

    return std::vector<unsigned char>();
}
